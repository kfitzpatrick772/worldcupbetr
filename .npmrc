# Prisma generates its client into src/generated/prisma (outside node_modules),
# and that code require()s @prisma/* internals. Hoist @prisma/* to the root
# node_modules so the generated client resolves them under pnpm's strict layout.
public-hoist-pattern[]=@prisma/*
confirm-modules-purge=false
